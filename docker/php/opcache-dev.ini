; Development OPcache overrides — installed by the development Dockerfile stage
; on top of the production opcache.ini. Never loaded in production.
; See docs/adr/009-hot-reloading.md#decision-dockerfile

[opcache]
; Re-enable file stat checks so OPcache does not serve stale bytecode after a source edit.
; Production keeps this at 0 for throughput. See docs/adr/009-hot-reloading.md#decision-opcache
opcache.validate_timestamps=1
; Check on every request rather than on a timer, so changes take effect immediately.
; See docs/adr/009-hot-reloading.md#decision-opcache
opcache.revalidate_freq=0
; Preloading is disabled: it would lock compiled bytecode at worker startup,
; defeating the timestamp validation above. See docs/adr/009-hot-reloading.md#consequences-preload
opcache.preload=
opcache.jit=tracing
opcache.jit_buffer_size=64M
