<?php

namespace Fixture;

// 'yes' is only 3 chars — below MIN_LENGTH of 4, never flagged.
$answer = 'yes';
$flag = 'on';

?>
-----
<?php

namespace Fixture;

// 'yes' is only 3 chars — below MIN_LENGTH of 4, never flagged.
$answer = 'yes';
$flag = 'on';

?>
