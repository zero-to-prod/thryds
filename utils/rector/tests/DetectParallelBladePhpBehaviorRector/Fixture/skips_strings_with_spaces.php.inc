<?php

namespace Fixture;

// Strings with spaces are natural-language phrases, not identifiers.
$message = 'alert-danger message to show';
$label = 'primary button label';

?>
-----
<?php

namespace Fixture;

// Strings with spaces are natural-language phrases, not identifiers.
$message = 'alert-danger message to show';
$label = 'primary button label';

?>
