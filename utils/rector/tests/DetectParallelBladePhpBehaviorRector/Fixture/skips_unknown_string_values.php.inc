<?php

namespace Fixture;

// 'foobar-xyz' is not in the pre-seeded registry — never flagged.
$value = 'foobar-xyz';
$other = 'some-unique-token';

?>
-----
<?php

namespace Fixture;

// 'foobar-xyz' is not in the pre-seeded registry — never flagged.
$value = 'foobar-xyz';
$other = 'some-unique-token';

?>
